spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yamada
    hikari:
      username: root
      password: 123456
      driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
management:
  endpoints:
    web:
      exposure:
        # 暴露所有端点
        include: "*"